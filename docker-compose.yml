version: '3'
services:
  nginx-router:
    build:
      context: ./nginx-router
      dockerfile: Dockerfile.dev
    ports:
      - '3000:3000'
  api-gateway:
    build:
      context: ./api-gateway
      dockerfile: Dockerfile.dev
